# Create an API key in the TypeSafe AI console. Never commit the real value.
TYPESAFE_API_KEY=
# Optional: SDK default is jev-latest. Pin a model before calibrating thresholds.
TYPESAFE_DEFAULT_MODEL=jev-latest
